{
    "name": "@react-freyja/monorepo",
    "description": "React component framework",
    "private": true,
    "scripts": {
        "build": "turbo run build --filter=./packages/*"
    },
    "devDependencies": {
        "turbo": "^1.3.4"
    }
}
